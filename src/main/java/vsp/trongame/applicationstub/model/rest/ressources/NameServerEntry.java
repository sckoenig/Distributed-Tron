package vsp.trongame.applicationstub.model.rest.ressources;

/**
 * REST Ressource for the REST name server.
 * @param name entry's name
 * @param address entry's address
 */
public record NameServerEntry(String name, String address) {
}
