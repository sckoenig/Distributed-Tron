### Entscheidungen
- prepare nimmt nur noch playercount an.
- RPC auf beim Name Server.


Vorschlag angenommen:

**UDP vs TCP nachrichten:**
- Vorschlag:
   - Alles, was schnell gehen muss, per UDP -> gerade nur das view update und handleKeyEvent (es ist ok, wenn eine Nachricht verloren geht)
   - Alles andere TCP -> nur am Anfang und Ende des Games, nicht so zeitkritisch. Jede Nachricht sollte ankommen.

**Receiver:**
- TCPReceiver und UDPReceiver oder ein Receiver mit TCP-Socket UND UDP-Socket?
- UDP und TCP können auf gleichem port lauschen. Nur eine Adresse muss beim NameServer registriert werden. 

**Sender:**
- Muss irgendwie entscheiden können, dass Nachricht per TCP oder UDP socket verschickt werden soll
- Vorschlag: 
   - ApplicationStub gibt das schon mit (Wichtigkeit der Nachrichten ist ja Game Logik, oder ... ?)
   - IRemoteInvocation enthält Enum InvocationType { RELIABLE, UNRELIABLE }
   - InvocationType wird mit übergeben -> invoke(..., type : InvocationType).

- fair starting positions:
    - Positionen fest kodieren, nicht berechnen
    - die Arena muss minimal 10x10 sein (in Config überprüfen!)

- Threads:
    - 


### Fragen
- udp: Paketgröße? Müssen wir uns darüber Gedanken machen?
- pakete: Reihenfolge sicherstellen?
    - A geschieht vor B
    - B kommt aber vor A an und wird ausgeführt. -> Serverstub weiß ja nicht, dass A auch noch kommt
    - Dann kommt A, was passiert mit A?
    - z.B. handleSteer
    - z.B. GameStates, COUNTDOWN muss vor RUNNING ankommen
    - Problem bei TCP ggf auch, je nachdem ob wir uns die Sockets merken

